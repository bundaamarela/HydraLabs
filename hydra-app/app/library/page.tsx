// Biblioteca — lista de sessões (Fase 3)
export default function LibraryPage() {
  return (
    <main style={{ padding: 32, color: 'var(--fg)' }}>
      <h1 style={{ fontSize: 20, fontWeight: 500, marginBottom: 8 }}>
        Biblioteca
      </h1>
      <p style={{ color: 'var(--fg-muted)', fontSize: 13 }}>
        Fase 3: lista de sessões será construída aqui.
      </p>
    </main>
  );
}
