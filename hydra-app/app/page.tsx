// Arena — ecrã principal (Fase 2)
export default function ArenaPage() {
  return (
    <main style={{ padding: 32, color: 'var(--fg)' }}>
      <h1 style={{ fontSize: 20, fontWeight: 500, marginBottom: 8 }}>
        Hydra Labs — Arena
      </h1>
      <p style={{ color: 'var(--fg-muted)', fontSize: 13 }}>
        Fase 2: UI da arena será construída aqui.
      </p>
    </main>
  );
}
